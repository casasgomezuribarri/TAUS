% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{create_survival_curve}
\alias{create_survival_curve}
\title{Kaplan-Meier survival curve}
\usage{
create_survival_curve(
  data,
  title,
  colors = NULL,
  linetypes = NULL,
  title_size = 20,
  conf_int = TRUE,
  group,
  time_var,
  event_var,
  xmax,
  legend = "none",
  median_surv = TRUE
)
}
\arguments{
\item{data}{A data frame containing the survival data.}

\item{title}{Plot title (string).}

\item{colors}{Optional. Named or unnamed character vector of colours for
each group level. Required if \code{median_surv = TRUE}.}

\item{linetypes}{Optional. Named or unnamed character vector of linetypes
for each group level. If \code{NULL}, all lines are solid.}

\item{title_size}{Font size for the plot title. Defaults to 20.}

\item{conf_int}{If \code{TRUE}, adds a shaded confidence interval around
each survival curve. Defaults to \code{TRUE}.}

\item{group}{Name of the grouping variable (string). Use \code{NA} for an
overall survival curve with no grouping.}

\item{time_var}{Name of the time variable (string).}

\item{event_var}{Name of the event variable (string). Expected to be a
binary variable where 1 indicates the event (death) and 0 censoring.}

\item{xmax}{Maximum value for the x-axis.}

\item{legend}{Legend position. Passed to \code{ggplot2::theme()}.
Common values are \code{"none"}, \code{"bottom"}, \code{"right"}.
Defaults to \code{"none"}.}

\item{median_surv}{If \code{TRUE}, adds median survival annotations and
reference lines to the plot. Requires \code{colors} to be specified.
Defaults to \code{TRUE}.}
}
\value{
A \code{ggplot2} object, returned invisibly. The plot is also
printed as a side effect.
}
\description{
Fits and plots a Kaplan-Meier survival curve using \code{survfit2()} and
\code{ggsurvfit()}. Supports grouping, confidence intervals, custom colours
and linetypes, and optional median survival annotations.
}
\examples{
\dontrun{
# Overall survival curve, no grouping
create_survival_curve(
    data = my_data, title = "Overall survival",
    group = NA, time_var = "time", event_var = "status",
    xmax = 30, median_surv = FALSE
)

# Grouped survival curve with median annotations
create_survival_curve(
    data = my_data, title = "Survival by group",
    group = "treatment", time_var = "time", event_var = "status",
    colors = c("red", "blue"), xmax = 30
)
}
}
